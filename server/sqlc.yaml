version: "2"
sql:
  - engine: "sqlite"
    queries: "queries/*.sql"   # Путь к файлам с SQL-запросами
    schema: "schema/schema.sql" # Путь к файлу схемы БД
    gen:
      go:
        package: "queries"   # Пакет, в который будет генерироваться Go-код
        out: "queries" # Папка, в которую будет генерироваться Go-код
